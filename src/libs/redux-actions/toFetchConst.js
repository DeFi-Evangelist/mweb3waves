const fetchConst = constant => ({
    CLEAR: `${constant}_CLEAR`,
    ERROR: `${constant}_ERROR`,
    REQUEST: `${constant}_REQUEST`,
    SUCCESS: `${constant}_SUCCESS`,
});

export default fetchConst;
